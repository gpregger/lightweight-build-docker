FROM alpine:latest

# Tools
RUN apk update && apk add --no-cache mkvtoolnix tree tzdata powershell

#CMD /scripts/config_container.sh && exec /bin/bash -c "trap : TERM INT; sleep infinity & wait"
CMD cat
